server:
  port: 0 # Port aléatoire pour les tests, c'est mieux

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/citymesh_crud?createDatabaseIfNotExist=true
    username: root
    password:       # <-- LAISSE VIDE (Comme pour XAMPP)
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect